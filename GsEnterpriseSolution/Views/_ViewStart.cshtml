@{
    Layout = "_Layout";
    // var cookies = Context.Request.Cookies;
    // var userData = cookies.Where(x => x.Key == "user-data").Select(x => x.Key).FirstOrDefault();

    // if(userData == null && (Context.Request.Path != "/" || Context.Request.Path != "/login"))
    // {
    //     Context.Response.Redirect("/");
    // }
}
